\documentclass[11pt]{article}

% Packages
\usepackage{amsmath,amssymb,amsthm} % Math stuff
\usepackage{enumitem} % For improved lists
\usepackage{fancyhdr} % For headers
\usepackage{geometry} % Page geometry
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{subcaption}
\geometry{margin=1in}
\usepackage{xcolor}
\usepackage{float}
\usepackage{mathtools}
\usepackage[colorlinks=true, 
    linkcolor=blue, 
    citecolor=blue, 
    urlcolor=blue, 
    filecolor=blue]{hyperref}

% Header
\pagestyle{fancy}
\fancyhf{}
\lhead{Asher Baraban, Matej Cerman}
\chead{14.383}
\rhead{Problem Set 5}
\cfoot{\thepage}

\newtheorem{theorem}{Theorem}[section]

% For easier problem/solution environments
\newenvironment{problem}[1]
    {\bigskip\noindent\textbf{Problem #1.}\par\setlength{\parindent}{0pt}}
    {\medskip}
\newenvironment{solution}[1]
    {\bigskip\noindent\textbf{Solution #1.} \par}
    {\medskip}

\newcommand\barbelow[1]{\stackunder[1.2pt]{$#1$}{\rule{.8ex}{.075ex}}}




% Math shortcuts
\newcommand{\ev}{\mathbb{E}}
\newcommand{\evn}{\mathbb{E}_n}
\newcommand{\R}{\mathbb{R}}

% Title
\title{Problem Set 5 \\ 14.383}
\author{Asher Baraban, Matej Cerman, Martin Hiti}
\date{\today}

% Document
\begin{document}


\maketitle

\noindent \textit{Notes}:
\begin{itemize}
    \item All code is in \url{https://github.com/ABaraban/14.383_pset5} and from the colab notebooks shared for the problem set 
    \item All three group members took 14.381 last semester
\end{itemize}

\subsection*{Chapter 11} 

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{problem}{11.2}
Replicate the empirical results of Tables 10.3 and 10.4 for the
fixed effects and jackknife BC using standard software such
as R or Stata.
\end{problem}

\begin{solution}{11.2}
We report the two tables below. In the first table, we show the estimated parameters from running a fixed-effects probit model in the PSID panel, where:
\begin{itemize}
    \item The outcome is $LPF_t$, the labor force participation of a woman at time $t$
    \item The Kids variables inlcude the number of children aged 0-2, 3-5, and 6-17 respectively
    \item Other controls include the log of the husband's income and a quadratic in age
    \item The model includes $LFP_{t-1}$, one lag of the outcome, as a control
\end{itemize}

The table has three columns, with the first corresponding to estimating a model with no bias correction, and the second to a model with analytical bias correction using a correction $\check \alpha = \hat \alpha - \frac{\hat B}{T} - \frac{\hat D}{N}$ with $\hat B,\hat D$ being estimates of the $B,D$ vectors that factor into the asymptotic bias.

The third column reports estimates with a Jackknife Bias Correction, implemented via sample-splitting repeated multiple times along the cross-sectional dimension. The correction estimates $\hat \alpha_{N/2,T}$ (an estimate using half the sample) multiple times, averages them, and then subtracts $\hat \alpha$ to get an estimate of the bias to subtract and obtain $\check \alpha$. Note that we ran into numerical issues in Stata with choosing $m=20$ splits as in the textbook, so the reported Jackknife estimates use $m=5$ splits. This is necessarily less stable, but the results are quantitatively extremely similar to the textbook. The magnitude of the estimated bias (difference between the raw and the bias-corrected estimates) is often on the order of the standard error of the APE, reported in the last column.

\begin{center}
\input{code/problem1/table1}
\end{center}

The second table reports the same model specifications, but the estimands of interest are APEs instead of the probit model coefficients. In general, the bias corrections appear to substantially raise the predictive power of $LFP_{t-1}$, the lagged term (if a woman worked in the previous year, she is $20$ p.p. more likely to work this year, vs $10$ p.p. with no bias correction), and somewhat raise the negative effect of children on labor force participation.


\begin{center}
\input{code/problem1/table2}
\end{center}

\end{solution}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%





%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newpage

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{problem}{2 }

\end{problem}

\begin{solution}{2}
\textbf{Neyman Orthogonality}
- In the double lasso approach the estimand of interest is $\alpha$ the treatment effect and $\eta^0 = (\gamma_{YW}', \gamma_{DW}')$ are the nuisance parameters obtained from partialling out. 
\[\hat{\gamma}_{YW} = \arg\min_{\gamma} \sum_{i}(Y_i - \gamma' W_i)^2 + \lambda_1 \sum_j\hat{\psi}_j|\gamma_j|\]
\[\hat{\gamma}_{DW} = \arg\min_{\gamma} \sum_{i}(D_i - \gamma' W_i)^2 + \lambda_2 \sum_j\hat{\psi}_j|\gamma_j|\]
Then, we obtain the residuals after partialling out, which I will denote as  $\tilde{Y}_i, \tilde{D}_i$. The double lasso exploits the empirical analogue of the following moment condition. 
\[g(\alpha, \eta) = E[(\tilde{Y}(\eta_1) - a \tilde{D}(\eta_2))\tilde{D}(\eta_2)]=0\]
Here the true parameter $a=\alpha$ solves the above moment equation when
\[\eta := (\eta_1', \eta_2') = \eta^0:= (\gamma_{YW}', \gamma_{DW}')
'\]
Now we consider the derivative of the score w.r.t to $\eta$
\[\partial_{\eta_1} g(\alpha, \eta^0) = E[W\tilde{D}] = 0, \partial_{\eta_2} = -E[\tilde{Y}W] + 2E[\alpha \tilde{D}W] = 0\]
These FOCs are Neyman Orthogonality. This has an envelope theorem style intuition. Small perturbations in the estimation of the nuisance parameters will not create large estimation issues for the empirical estimand of interest $\alpha$. At the optimum changing $\eta$ won't affect the score! Succinctly 
\[\partial_\eta g(\alpha, \eta^0) = 0\]

\textbf{Approximate Sparsity} - Informally, in order for lasso to work we need approximate sparsity of $\eta^0$. Formally, 
\[|\gamma_{YW}|_{(j)} \leq A_j^{-a}, |\gamma_{DW}|_{(j)} \leq A_j^{-a}\]
Where $a>1$ and $j \in \{1,...,p\}$

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{histogram_output_problem2.png}
\end{figure}
As we can see in the figure above, when we do not use double selection we end up with a severe bias as in general $E[DW]\neq 0$. But we do have that $E[\tilde{D}W] = 0$ from partialling out using lasso. This results in the beautiful normally distributed close to unbiased histogram on the right. 

    
\end{solution}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newpage
\begin{problem}{3}

\end{problem}

\begin{solution}{3}
We ran the notebook using Daron's Democracy example, and found some interesting results. The idea is that we want to understand how economic growth is related to the initial wealth level of countries controlling for institutions, democracy, etc. (as we know they have a causal effect on growth). 
\[ Y = \beta_1 D +  \beta_2'W + \epsilon\]
Since we have 63 controls and 90 countries $p/n$ is not small. Therefore, the least squares approach will not work well. Thus, we need to use a lasso method to recover a reasonable solution. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{problem_3_treatment_lasso_cv_plot.png}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{problem_3_outcome_lasso_cv_plot.png}
\end{figure}

From the figures above we can see that the cross validation overfits, using a penalty level very close to $0$. Therefore, we should prefer a theoretically chosen lasso parameter as in the the earlier part of the problem. 
\end{solution}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newpage
\begin{problem}{4}

\end{problem}

\begin{solution}{4}
The problem is set in the context of a IV structural model, which differs from the canonical IV model due to the non-linearity present in both equations ($g_0(X)$ is a potentially non-linear function of covariates $X$, capturing their effect on the outcome, and $m_0(X)$ captures a potentially non-linear relationship between those covariates and an instrument $Z$ which affects the treatment $D$).

\begin{align}
 Y :=~& D\theta_0 + g_0(X) + \zeta,  & E[\zeta \mid Z,X]= 0,\\
 Z :=~& m_0(X) +  V,   &  E[V \mid X] = 0.
\end{align}

We can write this model using a standard residualized form, with tilde variables denoting a residualization with respect to $X$: $\tilde Y = \tilde D \theta_0 + \zeta, \quad E[\zeta |V,X] = 0 $. Note that the residualization here is not done via linear regression as in Frisch-Waugh, but instead via a flexible ML algorithm that estimates the conditional mean of a variable as a non-linear function of $X$. In particular, the algorithm should employ cross-validation to reduce potential overfitting, particularly in a high-dimensional setting.

After we've used an ML algorithm to estimate $\tilde Y,\tilde D, \tilde Z$, we can run a standard linear 2SLS IV procedure on these residualized quantities, (conceptually) running a first-stage regression of $\tilde D$ on $\tilde Z$ and a reduced-form regression of $\tilde Y$ on $\tilde Z$, recovering an estimate of $\theta_0$ as the ratio of the two resulting coefficients.


\begin{center}
    \includegraphics[scale=0.6]{}
\end{center}


\end{solution}


\end{document}